<UserControl
    x:Class="StoryTeller.Controls.TagsListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoryTeller.Controls"
    xmlns:converter="using:StoryTeller.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">


    <UserControl.Resources>
        <ResourceDictionary>
            <converter:SceneTagToString x:Key="SceneTagToString" />
        </ResourceDictionary>
    </UserControl.Resources>

    <ListView Grid.Row="0" ItemsSource="{Binding  Tags}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Converter={StaticResource ResourceKey=SceneTagToString}}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
